node_modules/
dist/
.turbo/
.next/
.expo/
out/
coverage/
test-results/
playwright-report/
blob-report/

# env
.env
.env.local
.env.*.local
!.env.example

# supabase local
packages/db/supabase/.branches/
packages/db/supabase/.temp/

# os / editor
.DS_Store
*.log
*.tsbuildinfo

# claude code — project settings are shared, local overrides are not
.claude/settings.local.json

# Worktrees live here. They are checkouts of this same repository, so
# committing one embeds a git repo inside itself and clones come back empty.
.claude/worktrees/
